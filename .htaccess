# redirect html pages to the root domain

RewriteEngine On

RewriteRule /voorbereiding  /preparation.html    [NC,L]    # works if i change the href to pages/some
RewriteRule mecaniek  /mechanic.html    [NC,L]    # works if i change the href to pages/some
RewriteRule spuitcabines  /painting.html    [NC,L]    # works if i change the href to pages/some
RewriteRule monteer-demonteer  /assemble.html    [NC,L]    # works if i change the href to pages/some
RewriteRule auto-diagnose  /diagnose.html    [NC,L]    # works if i change the href to pages/some

RewriteRule autos  /carsPage.html    [NC,L]    # works if i change the href to pages/some
RewriteRule bestelwagens  /busesPage.html    [NC,L]    # works if i change the href to pages/some
RewriteRule motorfietsen  /motobikesPage.html    [NC,L]    # works if i change the href to pages/some

RewriteRule galerij  /gallery.html    [NC,L]    # works if i change the href to pages/some
RewriteRule contact-ons  /contact.html    [NC,L]    # works if i change the href to pages/some
